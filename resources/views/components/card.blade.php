<div class="card">
    <div class="card-header">
        {{$header}}
    </div>
    <div class="card-body">
        <h4 class="card-title">{{$title}}</h4>
        <p>{{$body}}</p>
        <a href="staffs" class="btn-btn-primary">See Detatails</a>
    </div>
</div>